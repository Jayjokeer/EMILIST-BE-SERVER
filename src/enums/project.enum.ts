export enum ProjectStatusEnum {
    pending = 'pending',
    accepted = 'accepted',
    rejected = 'rejected',
    completed = 'completed',
    unpause = "unpause",
    pause = "pause",
  }
  
export enum QuoteStatusEnum {
  pending = 'pending',
  accepted = 'accepted',
  rejected = 'rejected',
}