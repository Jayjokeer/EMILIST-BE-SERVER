export enum ProjectStatusEnum {
    pending = 'pending',
    accepted = 'accepted',
    rejected = 'rejected',
    completed = 'completed',
    unpause = "unpause",
  }
  
export enum QuoteStatusEnum {
  pending = 'pending',
  accepted = 'accepted',
  rejected = 'rejected',
}