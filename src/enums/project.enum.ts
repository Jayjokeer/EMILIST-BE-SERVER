export enum ProjectStatusEnum {
    pending = 'pending',
    accepted = 'accepted',
    rejected = 'rejected',
    completed = 'completed'
  }
  
export enum QuoteStatusEnum {
  pending = 'pending',
  accepted = 'accepted',
  rejected = 'rejected',
}